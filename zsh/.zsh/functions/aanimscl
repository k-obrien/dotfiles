# Set animation scale on Android device

function aanimscl() {
    emulate -L zsh
    adbsetput global transition_animation_scale "$1"
    adbsetput global window_animation_scale "$1"
    adbsetput global animator_duration_scale "$1"
}
